---
name: my-skill-name
description: Одно-два предложения о том, ЧТО делает скилл и КОГДА срабатывать. Начни с ключевого действия и добавь живые триггер-слова, по которым Claude поймёт, что нужен именно этот скилл («используй на запросы: …»).
metadata:
  author: Your Name
  version: 1.0.0
---

# my-skill-name — короткий заголовок

Один абзац: зачем скилл, какую проблему решает, чем отличается.

## Раздел с сутью

- Конкретные правила, а не общие слова.
- Числовые ориентиры, где уместно.

## Библиотека (опционально)

Тяжёлую глубину выноси в `references/*.md` и ссылайся на неё — так SKILL.md остаётся коротким, а детали читаются по требованию.

- `references/example.md` — о чём файл.

## Скрипты (опционально)

Исполняемое клади в `scripts/`. В плагине зови через `${CLAUDE_PLUGIN_ROOT}/skills/my-skill-name/scripts/tool.js`.
